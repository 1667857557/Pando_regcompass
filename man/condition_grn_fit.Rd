% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/condition_grn_output.R and
% R/zzz_condition_grn_condition_sparse.R
\name{condition_grn_fit}
\alias{condition_grn_fit}
\alias{condition_grn_fit.GRNData}
\title{Extract complete condition-aware GRN fit contracts}
\usage{
condition_grn_fit(object, network_name = NULL, cell_type = NULL)

\method{condition_grn_fit}{GRNData}(
  object,
  network_name = NULL,
  cell_type = NULL
)
}
\arguments{
\item{object}{A \code{GRNData} object returned by
\code{infer_condition_grn()}.}
\item{network_name}{Optional network prefix.}
\item{cell_type}{Optional cell-type label.}
}
\value{
One \code{ConditionGRNFit} object when exactly one fit matches, otherwise a
named list of matching objects. The v3 contract contains the shared candidate
edge dictionary, sparse-group selection coefficients, support-constrained
refitted condition coefficients, shared baseline and deviation layers,
eligibility, estimability and activity masks, reference contrasts, pooled
predictor and response transforms, and target-level selection/refit diagnostics.
}
\description{
Extracts the versioned fitted-model contract stored by
\code{infer_condition_grn()}. It is the lossless interface for downstream
condition comparisons; standard Pando \code{Network} objects remain available
for existing visualization and analysis functions.
}
\details{
For schema \code{pando_condition_grn_fit_v3}, the principal coefficient fields
are:

\itemize{
  \item \code{beta_selection}: sparse-group coefficients used to select support;
  \item \code{beta_condition}: refitted absolute condition effects, with
  unavailable entries represented as \code{NA};
  \item \code{beta_shared}: estimability-aware shared baseline;
  \item \code{delta_condition}: condition effect minus the shared baseline;
  \item \code{active_mask}: condition-specific active support;
  \item \code{estimability_mask}: whether an edge can be estimated in a condition.
}

The compatibility \code{beta} matrix contains the refitted effects but retains
zeros for unavailable entries. Downstream code requiring biological comparisons
should use \code{beta_condition} together with \code{estimability_mask} and
\code{comparison_mask}. Absolute direction is \code{sign(beta_condition)};
deviation direction is \code{sign(delta_condition)}.

Older v2 contracts remain readable. Their \code{contrast} field is
\code{beta_condition - beta_reference}, and they do not contain the explicit
shared/deviation/refit layers introduced by the condition-sparse engine.
}