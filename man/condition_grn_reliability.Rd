% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/condition_grn_reliability.R
\name{condition_grn_reliability}
\alias{condition_grn_reliability}
\title{Compute target-level reliability for condition GRN fits}
\usage{
condition_grn_reliability(fit, significant_only = TRUE)
}
\arguments{
\item{fit}{A \code{ConditionGRNFit} returned by \code{condition_grn_fit()}.}

\item{significant_only}{If \code{TRUE}, an active target requires at least one coefficient with \code{significant == TRUE}. If \code{FALSE}, at least one estimable coefficient is sufficient.}
}
\value{
A data frame with one row per target-condition fit and columns \code{target}, \code{condition}, \code{rsq}, \code{fit_status}, \code{n_active_edges}, and \code{reliability}.
}
\description{
Converts the target-by-condition goodness-of-fit statistic stored by a common-dictionary condition GRN into an amplitude-scale reliability. Reliability is defined as \code{sqrt(pmin(1, pmax(0, rsq)))} for \code{fit_status == "ok"} target-condition fits that retain at least one active edge. Targets without an active edge, with a non-ok fit status, or with non-finite R-squared remain unavailable (\code{NA}).
}
