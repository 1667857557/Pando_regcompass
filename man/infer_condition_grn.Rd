% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/zzz_condition_grn_condition_sparse.R
\name{infer_condition_grn}
\alias{infer_condition_grn}
\alias{infer_condition_grn.GRNData}
\title{Infer shared-baseline, condition-specific gene regulatory networks}
\usage{
infer_condition_grn(object, ...)

\method{infer_condition_grn}{GRNData}(
  object,
  cell_type_col,
  condition_col,
  genes = NULL,
  network_name = "condition_grn",
  peak_to_gene_method = c("Signac", "GREAT"),
  upstream = 1e+05,
  downstream = 0,
  extend = 1e+06,
  only_tss = FALSE,
  peak_to_gene_domains = NULL,
  tf_cor = 0.1,
  peak_cor = 0,
  candidate_screen = c("pooled_within_condition", "pooled", "motif_domain",
    "condition_union"),
  aggregate_rna_col = NULL,
  aggregate_peaks_col = NULL,
  method = c("shared_baseline_condition_sparse", "shared_design_independent",
    "multitask_glmnet"),
  family = "gaussian",
  interaction_term = ":",
  alpha = 0.5,
  condition_mix = 0.5,
  reference_condition = NULL,
  condition_weight = c("equal", "cell_count"),
  nlambda = 50L,
  lambda = NULL,
  lambda_min_ratio = NULL,
  nfolds = 5L,
  lambda_selection = c("lambda.1se", "lambda.min"),
  min_cells_per_condition = 50L,
  on_small_condition = c("skip_cell_type", "drop_condition", "error"),
  scale = TRUE,
  active_tol = 1e-08,
  parallel = FALSE,
  BPPARAM = NULL,
  overwrite = FALSE,
  seed = 12345L,
  max_iter = 5000L,
  tol_objective = 1e-07,
  tol_coef = 1e-06,
  verbose = TRUE,
  ...
)
}
\arguments{
\item{object}{A \code{GRNData} object containing paired RNA and ATAC measurements.}
\item{cell_type_col}{Metadata column containing cell-type labels.}
\item{condition_col}{Metadata column containing condition labels.}
\item{genes}{Target genes. Defaults to RNA variable features.}
\item{network_name}{Prefix used for generated network names.}
\item{peak_to_gene_method}{Peak-to-gene method, either \code{Signac} or \code{GREAT}.}
\item{upstream, downstream, extend, only_tss}{Regulatory-domain parameters passed to Pando peak-to-gene mapping.}
\item{peak_to_gene_domains}{Optional custom gene regulatory domains.}
\item{tf_cor, peak_cor}{Correlation screening thresholds.}
\item{candidate_screen}{Use \code{pooled_within_condition} (default) to
remove condition means before pooled correlation screening. \code{pooled},
\code{motif_domain}, and legacy \code{condition_union} are also available.}
\item{aggregate_rna_col, aggregate_peaks_col}{Optional shared aggregation label.
The default condition-sparse workflow is designed for single-cell input; both
values must be NULL or identical.}
\item{method}{Use \code{shared_baseline_condition_sparse} (default) for
sparse-group support selection followed by a support-constrained hierarchical
ridge refit. \code{shared_design_independent} and \code{multitask_glmnet} are
retained for compatibility.}
\item{family}{Currently must be \code{gaussian}.}
\item{interaction_term}{Currently must be \code{:}, matching the default Pando interaction.}
\item{alpha}{Elastic-net mixing parameter used during sparse-group support selection.}
\item{condition_mix}{Mixing between edge-row group sparsity and
condition-specific element sparsity. Values greater than zero permit different
condition-specific active edge sets.}
\item{reference_condition}{Condition used for stored compatibility contrasts.
Absolute condition coefficients and shared/deviation layers do not depend on
using the reference as the model baseline.}
\item{condition_weight}{Use equal-condition or cell-count loss weighting.}
\item{nlambda}{Number of lambda values when \code{lambda} is not supplied.}
\item{lambda}{Optional fixed lambda value or lambda path.}
\item{lambda_min_ratio}{Smallest lambda relative to lambda maximum.}
\item{nfolds}{Number of condition-stratified cell folds.}
\item{lambda_selection}{Select \code{lambda.1se} or \code{lambda.min}.}
\item{min_cells_per_condition}{Minimum cells per condition.}
\item{on_small_condition}{Action when a cell type contains a condition below the minimum.}
\item{scale}{Whether to standardize each TF-expression by peak-accessibility
predictor and target once across all conditions of a cell type. The refit and
all exported primary effects remain on this common scale.}
\item{active_tol}{Numerical threshold used for active-edge network summaries.}
\item{parallel}{Use the existing Pando foreach mapping backend.}
\item{BPPARAM}{Optional \code{BiocParallel} parameter object.}
\item{overwrite}{Replace generated networks with matching names.}
\item{seed}{Random seed for condition-stratified folds.}
\item{max_iter, tol_objective, tol_coef}{Sparse-group solver controls.}
\item{verbose}{Display progress messages.}
\item{...}{Reserved for future extensions.}
}
\value{
A \code{GRNData} object. For each eligible cell type, the object contains one
shared-baseline compatibility network and one condition network. The complete
\code{pando_condition_grn_fit_v3} contract is available through
\code{condition_grn_fit()} and contains selection, refitted absolute condition,
shared baseline, deviation, mask, transform and diagnostic layers.
}
\description{
Fits paired RNA+ATAC single-cell TF-expression by motif-bearing peak-accessibility
models on one shared candidate supergraph. The default engine permits different
condition-specific active nodes and edges, different effect magnitudes, and sign
reversals. Sparse-group coefficients select support; a support-constrained
hierarchical-ridge refit supplies the primary comparable effects.
}
\details{
The default \code{pooled_within_condition} screen removes condition means before
calculating a pooled correlation, reducing edges driven only by between-condition
shifts. All conditions retain the same candidate TF-peak-target dictionary.
Condition-specific unavailability is represented by \code{estimability_mask};
estimated inactive edges remain exactly zero through the support-constrained
refit.

The fit contract defines
\code{beta_condition = beta_shared + delta_condition}. Absolute direction is
\code{sign(beta_condition)}; deviation direction is
\code{sign(delta_condition)}. A negative deviation does not necessarily imply a
negative absolute regulatory effect.

Pando performs this inference on single cells. Metacells are not constructed by
this function and should only be introduced downstream by RegCompass after the
GRN coefficients and masks have been fixed.
}
\examples{
\dontrun{
object <- infer_condition_grn(
  object,
  cell_type_col = "cell_type",
  condition_col = "condition",
  genes = metabolic_genes,
  candidate_screen = "pooled_within_condition",
  method = "shared_baseline_condition_sparse",
  alpha = 0.5,
  condition_mix = 0.5,
  reference_condition = "Control",
  scale = TRUE
)

fit <- condition_grn_fit(object, cell_type = "Tumor_cell")
fit$beta_shared
fit$beta_condition
fit$delta_condition
fit$active_mask
fit$estimability_mask
}
}