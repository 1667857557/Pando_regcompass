% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/condition_grn.R and R/zzzzz_condition_multitask_ridge_api.R
\name{infer_condition_grn}
\alias{infer_condition_grn}
\alias{infer_condition_grn.GRNData}
\title{Infer comparable condition-specific Pando GRNs}
\usage{
infer_condition_grn(object, ...)

\method{infer_condition_grn}{GRNData}(
  object,
  cell_type_col = NULL,
  condition_col = NULL,
  cell_type = NULL,
  genes = NULL,
  network_name = "condition_grn",
  peak_to_gene_method = c("Signac", "GREAT"),
  upstream = 1e+05,
  downstream = 0,
  extend = 1e+06,
  only_tss = FALSE,
  peak_to_gene_domains = NULL,
  rna_layer = "data",
  peak_layer = "data",
  peak_value_type = c("normalized", "probability", "other"),
  tf_cor = 0.1,
  peak_cor = 0,
  min_cells_per_condition = 50L,
  small_condition_action = c("error", "drop_condition", "skip_cell_type"),
  adjust_method = "BH",
  padj_threshold = 0.05,
  rank_action = c("mark", "error"),
  min_residual_df = 1L,
  ridge_control = list(),
  parallel = FALSE,
  BPPARAM = NULL,
  parallel_scope = c("auto", "cell_type", "target"),
  overwrite = FALSE,
  fallback_args = list(),
  verbose = TRUE,
  ...
)
}
\arguments{
\item{object}{A \code{GRNData} object after motif matching.}
\item{cell_type_col}{Broad cell-type metadata column.}
\item{condition_col}{Condition column. NULL, absent, or fewer than two levels selects original per-cell-type Pando.}
\item{cell_type}{Optional cell-type subset.}
\item{genes}{Target genes.}
\item{network_name}{Prefix for generated networks.}
\item{peak_to_gene_method, upstream, downstream, extend, only_tss, peak_to_gene_domains}{Regulatory-domain controls.}
\item{rna_layer, peak_layer, peak_value_type}{RNA and ATAC layers and peak-value semantics used consistently for candidate discovery, fitting, and projection.}
\item{tf_cor, peak_cor}{Candidate-discovery correlation thresholds.}
\item{min_cells_per_condition}{Minimum paired cells per fitted condition.}
\item{small_condition_action}{Action for undersized conditions.}
\item{adjust_method, padj_threshold}{Multiple-testing method and threshold defining \code{penalty_effect}.}
\item{rank_action}{With the default \code{"mark"}, raw rank deficiency is retained as a diagnostic because the regularized system remains identifiable. \code{"error"} requests strict failure when a raw condition design is rank deficient.}
\item{min_residual_df}{Minimum effective residual degrees of freedom for the joint regularized fit.}
\item{ridge_control}{Optional named list controlling the multi-task ridge refit. Supported fields are \code{lambda_grid} (default \code{10^seq(-3, 2, length.out = 9)}), \code{lambda_rule} (\code{"1se"} or \code{"min"}), \code{fusion_ratio} (default 1), \code{cv_folds} (default 5), \code{seed} (default 1), and \code{scale_floor} (default \code{1e-8}).}
\item{parallel}{Enable parallel execution.}
\item{BPPARAM}{\code{NULL}, \code{FALSE}, or a \code{BiocParallelParam}. When multiple cell types are requested, this controls independent cell-type jobs.}
\item{parallel_scope}{Parallelization level. \code{"auto"} uses cell-type jobs when more than one cell type is available; \code{"cell_type"} requests that behavior explicitly; \code{"target"} retains target-level execution.}
\item{overwrite}{Replace generated networks with matching names.}
\item{fallback_args}{Arguments passed only to original \code{infer_grn()} fallback when fewer than two conditions are available.}
\item{verbose}{Display progress messages.}
\item{...}{Must be empty.}
}
\value{A \code{GRNData} containing standard Pando \code{Network} objects and common-dictionary condition fit contracts.}
\description{For each multi-condition cell type, Pando keeps the existing global-plus-condition candidate discovery and exact TF-peak-target union, then fits the complete frozen dictionary with one joint multi-task ridge model per target.}
\details{
For edge \eqn{e} in condition \eqn{k}, the predictor remains the original Pando interaction
\eqn{x_{cek}=RNA_{TF_e,c} ATAC_{peak_e,c}}. The exact union dictionary is not pruned because of collinearity. Predictor centering and scaling are learned from the pooled cells of all fitted conditions; cross-validation relearns those transforms within each training fold. Stored coefficients are back-transformed to the original TF-by-ATAC units before projection.

For \eqn{K} conditions, the numerical objective is proportional to
\deqn{
\frac{1}{K}\sum_k\frac{||y_k-X_k\beta_k||_2^2}{n_k} +
\lambda\left\{\sum_k||\beta_k||_2^2 +
\frac{\gamma}{K-1}\sum_{k<l}||\beta_k-\beta_l||_2^2\right\}.
}
Each condition therefore contributes equal total loss weight. The positive ridge term makes the penalized system identifiable even when the raw common-dictionary design is rank deficient. The normalized fusion term borrows strength across conditions but does not force equality or delete an edge.

One \eqn{\lambda} is selected for all conditions of a target by condition-stratified cross-validation. The default one-standard-error rule chooses the largest lambda within one standard error of minimum equal-condition validation MSE. The public target-condition \code{rsq} field is out-of-fold R-squared; \code{rsq_in_sample} is retained separately.

The raw design rank, raw condition number, regularized condition number, effective degrees of freedom, condition-specific zero-variance flags, common lambda and CV loss are retained as diagnostics. A predictor that varies in the pooled common reference but is constant within one condition may receive a finite coefficient through the fusion penalty, but that condition-edge is marked non-estimable, receives no P value, and cannot enter \code{penalty_effect}. This separates partial-pooling information from condition-local evidence.

Approximate ridge-Wald standard errors use a residual sandwich covariance conditional on the selected dictionary, lambda, and fusion penalty. P values are BH-adjusted separately in every condition. The external RegCompass-compatible contract remains \code{estimable & padj < padj_threshold}; \code{penalty_effect} is the raw-unit coefficient for significant edges and zero otherwise. The external fit contract retains \code{scale = FALSE} because the exported coefficient is already back-transformed to raw TF-by-ATAC units.

When the condition column is absent or has fewer than two levels, the original per-cell-type Pando \code{infer_grn()} fallback is unchanged.
}
