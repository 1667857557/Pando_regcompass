% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/generics.R, R/regions.R
\name{find_motifs}
\alias{find_motifs}
\alias{find_motifs.GRNData}
\title{Scan for motifs in candidate regions.}
\usage{
find_motifs(object, ...)

\method{find_motifs}{GRNData}(
  object,
  pfm,
  genome,
  motif_tfs = NULL,
  verbose = TRUE,
  exact_positions = FALSE,
  cache_dir = getOption("Pando.motif_cache_dir", NULL),
  reuse_cache = TRUE
)
}
\arguments{
\item{pfm}{A \code{PFMatrixList} object with position weight matrices.}

\item{genome}{A \code{BSgenome} object with the genome of interest.}

\item{motif_tfs}{A data frame matching motifs with TFs. The first column is assumed
to be the name of the motif, the second the name of the TF.}

\item{exact_positions}{Whether to retain the exact genomic positions of motif
matches. The default, \code{FALSE}, stores only the sparse motif
presence/absence matrix used to construct TF-peak-target candidates. Set to
\code{TRUE} when exact match positions are needed, for example for TF
footprinting.}

\item{cache_dir}{Optional directory for persistent motif-match caching. Cache
entries are keyed by the ordered candidate ranges, complete PFM collection,
genome identity and \code{exact_positions}. Identical region layouts therefore
reuse one motif scan across cell types and later runs.}

\item{reuse_cache}{Logical. Read a valid existing cache entry before scanning.
A newly computed entry is still written when this is \code{FALSE}.}

\item{verbose}{Display messages.}
}
\value{
A GRNData object with updated motif info.
}
\description{
Scan for motifs in candidate regions.
}
