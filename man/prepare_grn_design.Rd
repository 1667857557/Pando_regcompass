% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/grn_design.R
\name{prepare_grn_design}
\alias{prepare_grn_design}
\alias{prepare_grn_design.GRNData}
\title{Prepare a shared pre-fit TF-peak-gene design}
\usage{
prepare_grn_design(object, ...)

\method{prepare_grn_design}{GRNData}(
  object,
  genes = NULL,
  peak_to_gene_method = c("Signac", "GREAT"),
  upstream = 1e+05,
  downstream = 0,
  extend = 1e+06,
  only_tss = FALSE,
  peak_to_gene_domains = NULL,
  screen_method = c("structural", "union_within_group_correlation"),
  screen_group_col = NULL,
  tf_cor = 0,
  peak_cor = 0,
  min_tf_detection = 0.01,
  min_peak_detection = 0.01,
  min_target_detection = 0.01,
  max_edges_per_target = Inf,
  verbose = TRUE
)
}
\arguments{
\item{object}{A \code{GRNData} object after \code{find_motifs()}.}
\item{...}{Arguments passed to an object-specific method.}
\item{genes}{Target genes. Defaults to RNA variable features.}
\item{peak_to_gene_method}{Peak-to-gene method.}
\item{upstream, downstream, extend, only_tss}{Regulatory-domain parameters.}
\item{peak_to_gene_domains}{Optional supplied regulatory domains.}
\item{screen_method}{Structural candidates or union-within-group correlation screening.}
\item{screen_group_col}{Metadata column defining screening groups.}
\item{tf_cor, peak_cor}{Absolute grouped correlation thresholds.}
\item{min_tf_detection, min_peak_detection, min_target_detection}{Minimum positive-signal detection fractions.}
\item{max_edges_per_target}{Optional deterministic candidate cap per target.}
\item{verbose}{Display progress messages.}
}
\value{
A \code{PandoGRNDesign} list containing a shared candidate edge universe,
explicit RNA/ATAC source feature mappings, diagnostics and a matrix contract.
}
\description{
Builds one structural TF-peak-target candidate universe before coefficient
estimation. Every downstream condition can therefore use identical design
columns. Structural mode does not use pooled target-RNA correlation and does
not discard condition-specific sign reversals by pooled cancellation.
}
