% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/grn_design.R
\name{prepare_grn_design}
\alias{prepare_grn_design}
\alias{prepare_grn_design.GRNData}
\title{Prepare a shared Pando TF-peak-target candidate design}
\usage{
prepare_grn_design(object, ...)

\method{prepare_grn_design}{GRNData}(
  object,
  genes = NULL,
  peak_to_gene_method = c("Signac", "GREAT"),
  upstream = 1e+05,
  downstream = 0,
  extend = 1e+06,
  only_tss = FALSE,
  peak_to_gene_domains = NULL,
  min_tf_detection = 0,
  min_peak_detection = 0,
  min_target_detection = 0,
  max_edges_per_target = Inf,
  verbose = TRUE,
  ...
)
}
\arguments{
\item{object}{A \code{GRNData} object after \code{find_motifs()} has been run.}
\item{...}{Reserved for future design builders.}
\item{genes}{Target genes. Defaults to variable RNA features.}
\item{peak_to_gene_method}{Peak-to-gene domain method.}
\item{upstream, downstream, extend, only_tss}{Regulatory-domain arguments.}
\item{peak_to_gene_domains}{Optional custom regulatory domains.}
\item{min_tf_detection, min_peak_detection, min_target_detection}{Minimum fractions of cells with positive normalized signal, evaluated across the complete input object.}
\item{max_edges_per_target}{Optional deterministic structural edge cap per target.}
\item{verbose}{Display progress messages.}
}
\value{
A \code{PandoGRNDesign} list containing candidate edges, region mappings,
target diagnostics, a feature contract, parameters, and a design fingerprint.
}
\description{
Builds a condition-agnostic structural TF-peak-target edge universe before
model fitting. No coefficient, p-value, or pooled-expression significance
filter is applied.
}
