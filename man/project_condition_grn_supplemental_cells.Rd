% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/condition_grn_supplemental_projection.R
\name{project_condition_grn_supplemental_cells}
\alias{project_condition_grn_supplemental_cells}
\title{Project condition-specific supplemental OOF regulatory signal}
\usage{
project_condition_grn_supplemental_cells(
  object,
  fit = NULL,
  network_name = NULL,
  cell_type = NULL,
  scale = c("std", "raw"),
  output = c("gene_score", "edge_contribution"),
  targets = NULL,
  nonestimable = c("structural_zero", "error"),
  active_tol = if (is.null(fit)) 1e-08 else fit$active_tol
)
}
\arguments{
\item{object}{GRNData object containing the paired cells used for inference.}

\item{fit}{Optional canonical \code{pando_condition_grn_fit} object.}

\item{network_name, cell_type}{Optional fit filters when \code{fit} is omitted.}

\item{scale}{Standardized or raw coefficient scale.}

\item{output}{Return target scores or target scores plus edge contributions.}

\item{targets}{Optional target subset.}

\item{nonestimable}{Use structural zeros or stop on unavailable effects.}

\item{active_tol}{Activity threshold used in metadata.}
}
\value{
A \code{ConditionGRNProjection} explicitly marked as supplemental rather than primary.
}
\description{
Uses each condition's own outer-fold estimability mask. Edges unique to one condition can contribute to a supplemental penalty route while pairwise/global common-support projection remains the primary comparison.
}
