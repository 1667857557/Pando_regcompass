% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/generics.R, R/grn.R and late runtime adapters
\name{infer_grn}
\alias{infer_grn}
\alias{infer_grn.GRNData}
\title{Infer a Gene Regulatory Network with \code{Pando}}
\usage{
infer_grn(object, ...)

\method{infer_grn}{GRNData}(
  object,
  genes = NULL,
  network_name = paste0(method, "_network"),
  peak_to_gene_method = c("Signac", "GREAT"),
  upstream = 1e+05,
  downstream = 0,
  extend = 1e+06,
  only_tss = FALSE,
  parallel = FALSE,
  tf_cor = 0.1,
  peak_cor = 0,
  aggregate_rna_col = NULL,
  aggregate_peaks_col = NULL,
  method = c("glm", "ridge", "glmnet", "cv.glmnet", "brms", "xgb",
    "bagging_ridge", "bayesian_ridge"),
  alpha = 0.5,
  family = "gaussian",
  interaction_term = ":",
  adjust_method = "fdr",
  scale = FALSE,
  verbose = TRUE,
  BPPARAM = NULL,
  ridge_control = list(),
  rank_action = c("mark", "error"),
  min_residual_df = 1L,
  padj_threshold = 0.05,
  ...
)
}
\arguments{
\item{...}{Other parameters for the model fitting function.}

\item{genes}{A character vector with the target genes to consider for GRN inference.
Takes all VariableFeatures in the object per default.}

\item{peak_to_gene_method}{Character specifying the method to
link peak overlapping motif regions to nearby genes. One of 'Signac' or 'GREAT'.}

\item{upstream}{Integer defining the distance upstream of the gene to consider as potential regulatory region.}

\item{downstream}{Integer defining the distance downstream of the gene to consider as potential regulatory region.}

\item{extend}{Integer defining the distance from the upstream and downstream of the basal regulatory region.
Only used if \code{peak_to_gene_method = 'GREAT'}.}

\item{only_tss}{Logical. Measure distance from the TSS (\code{TRUE}) or from the entire gene body (\code{FALSE}).}

\item{parallel}{Logical. Whether to parallelize independent target-gene fits. For \code{method = "ridge"}, a supplied \code{BPPARAM} is used directly.}

\item{tf_cor}{Threshold for TF-target gene correlation.}

\item{peak_cor}{Threshold for binding peak-target gene correlation.}

\item{method}{A character string indicating the method to fit the model. \code{"glm"} remains the default.
\itemize{
\item \code{'glm'} - Generalized Linear Model with \code{stats::glm()}.
\item \code{'ridge'} - Single-task ridge using the same CV, predictor scaling, regularized linear solver, effective degrees of freedom and ridge-Wald diagnostics as the condition-GRN multi-task ridge. With one task the fusion penalty is identically zero.
\item \code{'glmnet'}, \code{'cv.glmnet'} - Regularized Generalized Linear Model with \code{glmnet}.
\item \code{'brms'} - Bayesian regression using \code{brms}.
\item \code{'xgb'} - Gradient boosting regression using \code{xgboost}.
\item \code{'bagging_ridge'} - Bagging ridge regression using scikit-learn via \code{reticulate}.
\item \code{'bayesian_ridge'} - Bayesian ridge regression using scikit-learn via \code{reticulate}.
}}

\item{alpha}{The elastic-net mixing parameter used by glmnet-based methods.}

\item{family}{Error distribution and link. Standard ridge requires Gaussian identity.}

\item{interaction_term}{The interaction term between TF and binding site. RegCompass-compatible ridge requires \code{':'}.}

\item{adjust_method}{Method for adjusting p-values.}

\item{scale}{Logical. Whether to z-transform expression and accessibility matrices. Standard ridge requires \code{FALSE}; it uses its own training-fold predictor scaling internally and exports coefficients in raw TF-by-ATAC units.}

\item{verbose}{Logical. Display messages. Set verbose to '2' to print errors for all model fits.}

\item{BPPARAM}{Optional \code{BiocParallelParam} used by standard \code{method = "ridge"} target-level parallelism. The pool is stopped when the ridge call finishes if Pando started it.}

\item{ridge_control}{Named list for standard ridge. Supported fields match condition ridge: \code{lambda_grid}, \code{lambda_rule}, \code{fusion_ratio}, \code{cv_folds}, \code{seed}, and \code{scale_floor}. For a single task, \code{fusion_ratio} has no numerical effect.}

\item{rank_action}{For standard ridge, \code{"mark"} retains regularized solutions when the raw design is rank deficient; \code{"error"} stops instead.}

\item{min_residual_df}{Minimum effective residual degrees of freedom for standard ridge.}

\item{padj_threshold}{Adjusted-P threshold stored with standard ridge diagnostics; default \code{0.05}.}
}
\value{
A GRNData object.
}
\description{
Infer a gene regulatory network with Pando. The original standard-Pando GLM remains the default. Optional standard ridge reuses the condition-GRN ridge numerical backend with one task, providing regularization against collinearity without changing the TF-by-ATAC predictor definition.
}
